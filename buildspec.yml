version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20

  pre_build:
    commands:
      - echo Logging in to Amazon ECR
      - aws --version

  build:
    commands:
      - echo Build started
      - docker build -t brain-tasks .

  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '**/*'
